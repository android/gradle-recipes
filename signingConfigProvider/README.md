# signingConfigProvider recipe

Postponing Signing Configuration setup to the execution phase using providers
## To Run
Just type `./gradlew app:assembleDebug`
You will be able to find the APK `app/build/outputs/apk/debug.apk` signed with the key provided in a provider.
